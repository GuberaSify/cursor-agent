# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly initialized repository (`cursor-agent`) with no application code yet. The repository currently contains only a `README.md`.

### Environment

- The development environment requires no special setup beyond standard git tooling.
- No dependencies to install, no services to run, no build steps needed.

### When code is added

Once application code is added to this repository, this section should be updated with:
- How to install dependencies
- How to run the application in development mode
- How to run tests and lint checks
- Any non-obvious caveats for local development
